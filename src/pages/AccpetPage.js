import AuthTemplate from "../components/auth/AuthTemplate";
import AcceptForm from "../containers/auth/AcceptForm";

const AcceptPage = () => {

    return (
        <AuthTemplate>
            <AcceptForm />
        </AuthTemplate>
    );
};

export default AcceptPage;